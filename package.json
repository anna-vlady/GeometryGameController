{
  "name": "proun-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build --workspace=@proun/engine && npm run dev --workspace=app",
    "build": "npm run build --workspace=@proun/engine && npm run build --workspace=app"
  },
  "overrides": {
    "lru-cache": "^10.4.3"
  }
}
